{
  "name": "ask-fredrik-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker backend for the Ask Fredrik portfolio assistant (Workers Free, Workers-AI-ready).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dev:noai": "wrangler dev --config wrangler.dev-noai.jsonc",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.0.0"
  }
}
